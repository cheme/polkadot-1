[package]
name = "polkadot-primitives"
version = "0.6.17"
authors = ["Parity Technologies <admin@parity.io>"]
edition = "2018"

[dependencies]
serde = { version = "1.0", optional = true, features = ["derive"] }
parity-scale-codec = { version = "1.0.5", default-features = false, features = ["bit-vec", "derive"] }
primitives = { package = "substrate-primitives", git = "https://github.com/cheme/substrate", default-features = false, branch = "overlay_change" }
application-crypto = { package = "substrate-application-crypto", git = "https://github.com/cheme/substrate", default-features = false, branch = "overlay_change" }
sr-api = { git = "https://github.com/cheme/substrate", branch = "overlay_change", default-features = false }
sr-version = { git = "https://github.com/cheme/substrate", default-features = false, branch = "overlay_change" }
rstd = { package = "sr-std", git = "https://github.com/cheme/substrate", default-features = false, branch = "overlay_change" }
runtime_primitives = { package = "sr-primitives", git = "https://github.com/cheme/substrate", default-features = false, branch = "overlay_change" }
polkadot-parachain = { path = "../parachain", default-features = false }
bitvec = { version = "0.14.0", default-features = false, features = ["alloc"] }
babe = { package = "paint-babe", git = "https://github.com/cheme/substrate", default-features = false, branch = "overlay_change" }

[dev-dependencies]
substrate-serializer = { git = "https://github.com/cheme/substrate", branch = "overlay_change" }
pretty_assertions = "0.5.1"

[features]
default = ["std"]
std = [
	"parity-scale-codec/std",
	"primitives/std",
	"sr-api/std",
	"rstd/std",
	"sr-version/std",
	"runtime_primitives/std",
	"serde",
	"polkadot-parachain/std",
	"bitvec/std",
	"babe/std"
]
